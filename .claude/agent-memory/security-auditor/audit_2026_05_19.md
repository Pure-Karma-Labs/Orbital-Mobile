---
name: audit-2026-05-19
description: Session audit summary — key zeroization, File Library, crypto tests, identityChanged fix
created: 2026-05-19
---

## Audit Summary — 2026-05-19

### PR #129 (Key Zeroization)
- `Zeroizing::new(keys)` ownership verified correct in attachment_crypto.rs
- `ct_eq` with length guard is safe; `lto=thin` doesn't elide zeroization
- Crate feature flags: aes/cbc/aes-gcm have zeroize; hmac does NOT (open item)
- sha2 has no zeroize feature (not keyed, acceptable)

### PR #132 (File Library)
- SQL allowlist (`SORT_COLUMNS`/`SORT_DIRS`) prevents ORDER BY injection
- `isDatabaseInitialized()` guards on all 4 new repo functions
- Download-on-tap (FileLibraryCell) prevents mass-download stampede
- `setMediaBatch` store-first guard protects in-flight downloads

### PR #138 (Crypto Tests)
- Flagged identityChanged characterization test as HIGH — was codifying security gap
- identityChanged: true should set Unverified, not Default (both decryptPreKeyMessage and establishSession)

### Issue #137 (Production Fix)
- RESOLVED: Both paths now set `VerifiedStatus.Unverified` when `identityChanged: true`
- Existing `first_use` and `nonblocking_approval` preserved
- Test updated from KNOWN GAP to asserting correct behavior

### New Patterns Identified
- SQL ORDER BY needs allowlist, not parameterized queries
- Auto-download hooks in unbounded lists = stampede risk
- Characterization tests must be marked KNOWN GAP, not "correct behavior"
- Zustand selectors must return stable references (not new objects) to avoid infinite loops
