version: "1.0"
last_updated: "2026-04-07"
agent: qa-testing-specialist

core_files:
  - path: package.json
    purpose: Testing dependencies — Jest config, testing library packages, Detox (to be added)
    key_symbols:
      - jest
      - scripts.test
    last_hash: fce34c2

  - path: .github/workflows/ci.yml
    purpose: CI test execution — Jest runs with --ci --coverage flags on self-hosted runner
    last_hash: c3743d9

  - path: src/types/database.ts
    purpose: TypeScript interfaces defining all data models — test fixtures should conform to these types
    last_hash: 6bd8bd6

  - path: src/database/migrations/001_initial_schema.sql
    purpose: Database schema — test setup must create tables matching this schema
    last_hash: 2445035

documentation:
  - path: docs/MOBILE-APP-SPEC.md
    relevance: "Part 6: Screens (acceptance criteria), Part 7: Implementation Phases (exit gates), Part 9: Risks (test priorities)"

integration_points:
  - system: Jest
    interface: npm test
    purpose: Unit and integration test runner — TypeScript, React Native preset

  - system: React Native Testing Library
    interface: @testing-library/react-native
    purpose: Component testing with user-centric queries

  - system: Detox (to be added)
    interface: E2E test framework
    purpose: End-to-end testing on iOS simulator and Android emulator

  - system: CI Pipeline
    interface: .github/workflows/ci.yml
    purpose: Automated test execution on every push and PR

patterns:
  - name: colocated-tests
    description: Test files live alongside source files (ComponentName.test.tsx next to ComponentName.tsx)

  - name: typed-fixtures
    description: Test fixtures conform to TypeScript interfaces from src/types/ — no untyped mock data

  - name: critical-path-priority
    description: "Test the critical user paths first: auth flow, thread creation, encryption round-trip, media upload"
