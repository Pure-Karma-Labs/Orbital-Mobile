# Local smoke testing — Android emulator reaches the host via 10.0.2.2.
# Debug builds only (release builds enforce https and block cleartext).
# No secrets; intentionally committed. See Orbital-Backend/docs/LOCAL-SMOKE.md.
API_URL=http://10.0.2.2:3000
SENTRY_DSN=
