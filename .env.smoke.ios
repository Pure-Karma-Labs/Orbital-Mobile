# Local smoke testing — iOS simulator shares the host network (localhost = host).
# Debug builds only; ATS NSAllowsLocalNetworking exempts localhost.
# No secrets; intentionally committed. See Orbital-Backend/docs/LOCAL-SMOKE.md.
API_URL=http://localhost:3000
SENTRY_DSN=
